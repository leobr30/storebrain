type  Objective = {
    id: number
    objective: number,
    objectiveOr: number,
    objectiveMode: number,
}

type PrimeDetail = {
    onerpId: number,
    fullname: string,
    zone: string,
    objective: number
}
